%ul.city
  - @locations.group_by(&:region_name).each do |region, region_locations|
    %li
      %h3{:id => region}
        = h region
        == (#{region_locations.size})
        %span.js-arrow
      %ul.locations
        - for location in region_locations.sort_by(&:name)
          %li[location]
            = link_to h(location.name), location
            &mdash;
            = location.address