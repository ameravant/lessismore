- content_for(:title) { concat "Edit Event" }
%h1
  = yield :title
- form_for :event, :url => [:admin, @event], :html => { :onsubmit => "form_submitted();", :method => :put } do |f|
  = render :partial => "form", :locals => { :f => f }
