- content_for :meta_title do
  = h @location.name

- content_for :crumb do
  == #{link_to "Dashboard", admin_path} &gt;
  == #{link_to "Locations", admin_locations_path} &gt;
  = yield :title

%h1= yield :title

- form_for [:admin, @location], :html => { :onsubmit => "form_submitted()" } do |f|
  = render :partial => "form", :locals => { :f => f }
